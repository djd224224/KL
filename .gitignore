Lisa_Kalshi.txt
*.pem
gcp_sa_key.json
__pycache__/
*.pyc

# Generated CSV exports from fetch_*_csv.py
Kalshi-Settlements-*.csv
Kalshi-Trades-*.csv
Kalshi-Orders-*.csv

# Dashboard HTML outputs (regenerated every skill run)
kalshi_dashboard_latest.html
weather_dashboard_latest.html
weather_report.html
kalshi_report.html

# Scheduled-tasks runtime lock file
.claude/scheduled_tasks.lock

# Local user settings (not repo-shared)
.claude/settings.local.json

# Crypto MM bot runtime logs (written by run_crypto_touch_mm.ps1)
run-logs/crypto-touch/
# Weekly one-touch variant (run_crypto_touch_mm_weekly.ps1). Separate dir so
# send_daily_digest.py's status_*.json glob never picks up weekly heartbeats.
run-logs/crypto-touch-weekly/
# Above/below variant (run_crypto_updown_mm.ps1) — separate dir for the same reason.
run-logs/crypto-updown/

# Gas bot runtime state + logs (aaa_history.csv IS tracked — model input)
gas_data/status_gas_bot.json
gas_data/HALT
run-logs/gas-bot/

# Migration bundle (migrate_export.ps1) — task XMLs + LIVE CREDENTIALS, never commit
migration_export/
